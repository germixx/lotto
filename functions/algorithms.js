// Algorith Goals / Rules

const algorithm1 = async (recentResultArr, hot, cold, overdue, repeat, filtered) => {
    let mostRecentLine = recentResultArr[recentResultArr.length - 1]

    return true

}

module.exports = {
    algorithm1
}